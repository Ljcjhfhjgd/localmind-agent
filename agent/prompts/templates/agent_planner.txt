将用户目标拆成1-3个子任务，尽量不要超过三个。如果只是打招呼/闲聊/感谢/简单问答，不需要任何工具，输出空数组 []。

需要工具时，每个子任务必须使用以下角色之一：{worker_names}

规则：
- 涉及编程/代码/实现 → 必须用 code
- 涉及搜索/查询/查找 → 必须用 search
- 涉及天气 → 必须用 weather
- 涉及翻译 → 必须用 translate
- 涉及数学计算/公式/几何 → 必须用 code（用 Python 计算），不要用 calculate
- 涉及时间 → 必须用 time
- 最后一步如果有多个结果需要验证 → 用 review

严格输出 JSON 数组，不要任何其他内容，不要 markdown 代码块。
格式示例：[{{"role": "search", "task": "搜索xxx"}}]

用户目标：{user_message}
JSON：